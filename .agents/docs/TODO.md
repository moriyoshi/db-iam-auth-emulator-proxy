# Verified open work

- Cloud SQL automatic IAM login through the Cloud SQL Auth Proxy or language connectors requires Cloud SQL Admin API and connector transport emulation. The current implementation supports manual token login.
- The mock endpoints implement the documented fixture paths and grants, not a full cloud control plane. Extend them only for concrete client compatibility cases.
